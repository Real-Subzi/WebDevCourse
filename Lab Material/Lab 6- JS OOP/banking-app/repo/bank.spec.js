//This spec file is the testing of bank.js
//you must teach the package.json to recognize these files.
//use npm test to do this testing. Or mocha **/*.spec.js

import * as repo from './bank.js';
import { expect } from 'chai';

describe('Add Account Function', ()=>{
    it('Adding an Account should increase the size of the array',()=>{
        const beforeAddingLength = repo.get
    })
})